package com.todolist.model;

/**
 * Enum representing task categories.
 * Used by TaskFactory (Factory Pattern) to determine default urgency behavior.
 */
public enum Category {
    PERSONAL,
    WORK,
    SHOPPING
}
